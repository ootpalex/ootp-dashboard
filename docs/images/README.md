# Screenshots used by `README.md` and `QUICKSTART.md`

| File | Source view |
|---|---|
| `hero.png` | My Organization → Overview |
| `roster-planner.png` | Roster Planner — full page (active roster, contracts, depth chart, R5 risk, queues) |
| `dev-analysis.png` | Dev Analysis — full page (age-vs-WAA scatter, curve tuning, FV impact, live prospect preview) |
| `prospects.png` | Prospects → The Board with FV tier badges |
| `quickstart-first-run.png` | Terminal screenshot of `run.py`'s first-time-setup prompt — used at the top of `QUICKSTART.md` |

Capture at the dev server's default viewport (`localhost:3000` in a maximized window, ~1440×900). PNG, no compression artifacts. Crop to the relevant region.

Optional: a short demo GIF (`demo.gif`) showing league switching or a smart-rank toggle in action. Tools: macOS QuickTime to record a screen, then [LICEcap](https://www.cockos.com/licecap/) or `ffmpeg` to convert to GIF. Keep it under 5 MB for GitHub README rendering.
